# Content Factory Hook Support

Runtime family: `node`

This directory was scaffolded to bootstrap a safe publish route before the first article is written.

Keep `target.yml`, `hooks/`, and this README in version control. Generated
reports, preview bundles, and article drafts are local working files and are
gitignored. Published article source belongs under `app/articles/content/`.
